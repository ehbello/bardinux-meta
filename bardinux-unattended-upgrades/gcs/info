# YAML 1.1

# Nombre del paquete (minúsculas-separado-con-guiones)
name: bardinux-unattended-upgrades

# Autor
author: Enrique Hernández Bello <quique@osl.ull.es>

# Priority
priority: optional

# Section
section: base

# Descripción corta
shortdesc: "automatic installation of package upgrades for Bardinux"

# Descipción larga, las lineas han de estar separadas por "\n "
longdesc: "This package configures the backend script for the APT::Periodic::Unattended-Upgrade for download and install all package upgrades automatically\n and unattended, taking care to only install packages from the\n configured APT source, recognizing also the Bardinux repositories."

# Versión del paquete
version: 1.3.9

# Cambios en esta versión
changes:
    - Changed the way to force unattended-upgrade when network up using upstart.
