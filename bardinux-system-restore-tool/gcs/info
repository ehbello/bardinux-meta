# YAML 1.1

# Nombre del paquete (minúsculas-separado-con-guiones)
name: bardinux-system-restore-tool

# Autor
author: Enrique Hernández Bello <quique@osl.ull.es>

# Priority
priority: optional

# Section
section: base

# Descripción corta
shortdesc: "This package doesnt have description yet"

# Descipción larga, las lineas han de estar separadas por "\n "
longdesc: "This package doesnt have description yet"

# Versión del paquete
version: 1.2~dev9

#config_extension: .bsrt.gcs

# Cambios en esta versión
changes:
    - Enabled cmd_cp in rsnapshot.conf
    - Changed interval name to 'last' in rsnapshot.conf
    - Disabled sync_first feature in rsnapshot.conf
    - Fixed backup order in rsnapshot.conf
    - Hidden output of passwd root.
    - Fixed mount/umount scripts.
    - Added Post-Invoke hook to run rsnapshot after Dpkg changes.
    - Changed allowed users to "Menú administrativo".
    - Added "Crear punto de restauración" menu entry to GRUB2 conf.
